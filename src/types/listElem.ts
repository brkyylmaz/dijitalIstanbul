

type PageListItem = {
    id: number;
    title: string;
    thumbnail_url: string;
    page_type: string;
    built_at: string;
    location_str: string;
    lang: string;
    trid: number;
  };

  export type {
    PageListItem
  }